# Local development bindings for Wrangler `dev --local`
# Populated with the Docker Postgres instance started via `pnpm docker:up`

DATABASE_URL="postgres://postgres:changeme@localhost:5432/arena"
ENVIRONMENT="development"
SENTRY_DSN=""
FRONTEND_ORIGIN="http://localhost:3000"
LOG_LEVEL=debug