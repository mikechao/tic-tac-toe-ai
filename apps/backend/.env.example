# Shared environment variables for deployment (set via Wrangler secrets or vars)

# Local Docker Postgres default; override with Hyperdrive in staging/production
DATABASE_URL="postgres://postgres:postgres@localhost:5432/tic_tac_toe_ai"
# DATABASE_URL="https://<hyperdrive-connection-string>"

ENVIRONMENT="production"
SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"
FRONTEND_ORIGIN="https://frontend.example.com"
